import { StyledReadingChallenge } from "../components/styles/ReadingChallenge.styled";

const ReadingChallenge = () => {
	return (
		<StyledReadingChallenge>
			<h2>Stay Tuned! A New Challenge is on its Way!</h2>
			<p>
				This page is under construction, but we can't wait to show you what's
				coming next.
			</p>
		</StyledReadingChallenge>
	);
};

export default ReadingChallenge;


