import styled from "styled-components";

export const StyledBookQuote = styled.div`
	blockquote {
		font-size: 0.9rem;
		font-style: italic;
		text-align: justify;
		margin: 0.625rem 0 1.25rem 0;
		padding-left: 0.938rem;
		border-left: 3px solid #ff9933;
	}
`;
